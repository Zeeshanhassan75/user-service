server:
  port: 8086

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/Microservices
    username: root
    driver-class-name: com.mysql.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    
